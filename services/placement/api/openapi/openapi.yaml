openapi: 3.0.3
info:
  title: Order Service APIs
  description: The api contract for order service.
  version: 1.0.0

servers:
  - url: http://localhost:8080

paths:
  /health:
    $ref: './paths/internal.yaml'

  /orders/{id}:
    $ref: './paths/orders_id.yaml'

  /orders:
    $ref: './paths/orders.yaml' 
